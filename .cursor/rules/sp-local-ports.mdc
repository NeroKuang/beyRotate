---
description: 本機 SP 專案開發 port 分配（5001 起）
alwaysApply: true
---

# SP 本機開發 Port 規範

`Desktop/SP` 底下專案的本機 dev server **固定從 5001 起排**，避免與 3000 上常見的其他服務（Open WebUI 等）衝突。

## 分配

| Port | 用途 |
|------|------|
| **5001** | 主要 Next.js 網站（BeyRotate 等 SP 主 app） |
| **5002+** | 同機其他 SP 專案或額外 dev server，依序往後 |

基礎設施（Docker）維持慣用 port，不占用 500x：

- Postgres `5432`、Redis `6379`、MinIO `9000` / 主控台 `9001`

## 設定時必同步

改 dev port 時，一併更新：

- `package.json` → `next dev -p 5001`
- `.env.local` / `.env.example` → `AUTH_URL`、`NEXTAUTH_URL`、`NEXT_PUBLIC_SITE_URL` 為 `http://localhost:5001`
- README 與文件中的本機網址

## 注意

- 不要用 `3000` 作為 SP 專案預設 port。
- 若 5001 已被占用，Next.js 可能自動改用 5002；此時須把 env 的 URL 改成實際 port，或釋放 5001。
