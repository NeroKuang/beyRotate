# 複製為 .env.local（本機）或在 Zeabur 後台 Variables 貼上對應欄位

# --- 資料庫 ---
# 本機：docker compose 預設帳密
DATABASE_URL=postgresql://beyrotate:beyrotate_dev@localhost:5432/beyrotate
# Zeabur：DATABASE_URL=${POSTGRES_CONNECTION_STRING}

# --- Auth（必填）---
# NextAuth v4 讀 NEXTAUTH_*；AUTH_* 為別名（dev 腳本會自動對應）
AUTH_SECRET=
AUTH_URL=http://localhost:5001
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:5001
NEXT_PUBLIC_SITE_URL=http://localhost:5001
# SP 本機 dev 固定 5001；同機其他 SP 專案用 5002、5003…
# Zeabur：AUTH_URL=${ZEABUR_WEB_URL}、NEXT_PUBLIC_SITE_URL=${ZEABUR_WEB_URL}

ADMIN_EMAILS=your@email.com

# --- 圖片上傳（Imgur 免費圖床）---
# 到 https://api.imgur.com/oauth2/addclient 申請 Client ID（選 Anonymous usage）
IMGUR_CLIENT_ID=

# --- Redis 搜尋快取（本機 docker compose 已含 redis 服務）---
REDIS_URL=redis://localhost:6379

# --- Email（選用，未設定則驗證連結只印到 console）---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=BeyRotate <noreply@beyrotate.app>

# --- 選用 ---
CRON_SECRET=
# 定期任務（需 CRON_SECRET，Header: Authorization: Bearer $CRON_SECRET）：
# GET /api/cron/expire-listings   — 公開刊登逾 14 天自動關閉並刪除上傳圖
# GET /api/cron/cleanup-images    — 備援清理已結束刊登／過期草稿圖片
# GET /api/cron/sync-catalog      — 同步產品目錄
# 天數見 src/lib/constants.ts（LISTING_PUBLISH_TTL_DAYS=14）
# 首次 Zeabur 部署可暫設 RUN_DB_PUSH=1，schema 同步後改回 0
